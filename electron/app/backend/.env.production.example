# Production MongoDB — required for packaged builds (copy to .env.production on build machine)
MONGO_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?appName=importease

# Never set ALLOW_LOCAL_AUTH in production builds.
# ALLOW_LOCAL_AUTH=true
